import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { admissions } from "@/lib/db/schema"; import { desc } from "drizzle-orm"; export async function GET() { try { const all = await db.query.admissions.findMany({ with: { patient: true, doctor: true, room: true, bed: true }, orderBy: [desc(admissions.admissionDate)] }); return NextResponse.json(all); } catch { return NextResponse.json({ error: "Failed" }, { status: 500 }); } } export async function POST(request: Request) { try { const body = await request.json(); const [adm] = await db.insert(admissions).values({ ...body, admissionId: "ADM-" + Date.now().toString(36), admissionDate: new Date() }).returning(); return NextResponse.json(adm, { status: 201 }); } catch { return NextResponse.json({ error: "Failed" }, { status: 500 }); } }
